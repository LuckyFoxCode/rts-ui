import { Layout } from 'components';
import { FC } from 'react';

export const App: FC = () => {
  return (
    <div className='App'>
      <Layout>React TypeScript UI Library</Layout>
    </div>
  );
};
